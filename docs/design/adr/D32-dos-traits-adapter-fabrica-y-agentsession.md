---
number: D32
title: "Dos traits: `Adapter` (fábrica) y `AgentSession` (sesión viva)"
status: accepted
revises: []
revised_by: []
---

# D32 — Dos traits: `Adapter` (fábrica) y `AgentSession` (sesión viva)

Capacidades constantes calculadas en probe; `kill` extermina el árbol de
procesos (base de `yunta cancel`); outcome del agente = telemetría (A7);
`mock` de primera clase para CI sin LLM y `yunta run --adapter mock`.
