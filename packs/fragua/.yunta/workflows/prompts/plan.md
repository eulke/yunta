Read the brief and the surrounding context. Register a task ledger at
`{{run.dir}}/artifacts/plan.yaml`: one task per independently-verifiable
unit of work, each with its own `scope` and at least one executable
`criteria` command. Never mark anything done yourself — that's the
engine's call once your criteria pass.
